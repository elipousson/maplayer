# check_gg errors with an informative message for invalid input

    Code
      check_gg("not a gg")
    Condition
      Error:
      ! `"not a gg"` must be a gg class object or a list of gg class objects, not the string "not a gg".

